const sortEn = [
    ['relevance', 'relevance'],
    ['newest', 'newest'],
];

const sortRu = [
    ['relevance', 'Актуальные'],
    ['newest', 'Новые'],
];

const sortOptions = { ru: sortRu, en: sortEn };

export default sortOptions;